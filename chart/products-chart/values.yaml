replicaCount: 2

image:
  repository: 659591640509.dkr.ecr.us-west-2.amazonaws.com/streamlinepay-prod-products-cna-microservice
  tag: bb2504e-116
  pullPolicy: IfNotPresent

service:
  type: ClusterIP

resources:
  requests:
    cpu: "250m"       # ✅ Quoted
    memory: "256Mi"   # ✅ Quoted
  limits:
    cpu: "500m"       # ✅ Quoted
    memory: "512Mi"   # ✅ Quoted

securityContext:
  runAsUser: 1001
  runAsGroup: 1001
  fsGroup: 1001

productsSecret:
  name: products-chart-secret
  dbUri: postgres://productadmin:productpass@postgresql.database.svc.cluster.local:5432/products
  jwtSecret: supersecretjwtkey

serviceAccount:
  create: true
  name: products-chart-sa
  automount: true

configMap:
  FEATURE_FLAG: "true"
  LOG_LEVEL: "debug"

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: "70"  # ✅ Quoted
