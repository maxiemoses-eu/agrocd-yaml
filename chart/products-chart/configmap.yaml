apiVersion: v1
kind: ConfigMap
metadata:
  # FIXED: Explicitly set to match the Deployment's 'products-config' expectations
  name: products-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "products.labels" . | nindent 4 }}
data:
  FEATURE_FLAG: {{ .Values.configMap.FEATURE_FLAG | quote }}
  LOG_LEVEL: {{ .Values.configMap.LOG_LEVEL | quote }}